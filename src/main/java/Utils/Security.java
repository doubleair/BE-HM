package Utils;

/**
 * 类Security.java的实现描述：加密解密
 * 
 * @author jizhi.qy 2017年11月9日 上午1:29:11
 */
public class Security {

    public static String decrypt(String encryptedData, String password) {
        // Cipher cipher = Cipher.getInstance("AES/CBC/PKCS7Padding");
        // try {
        // byte[] result = DES.encrypt(str.getBytes(),password);
        // System.out.println("加密后："+new String(result));
        // AES aes = new AES();
        // byte[] resultByte = aes.decrypt(Base64.decodeBase64(encryptedData), Base64.decodeBase64(sessionKey),
        // Base64.decodeBase64(iv));
        // if (null != resultByte && resultByte.length > 0) {
        // String userInfo = new String(resultByte, "UTF-8");
        // return rtnParam(0, userInfo);
        // }
        // } catch (InvalidAlgorithmParameterException e) {
        // e.printStackTrace();
        // } catch (UnsupportedEncodingException e) {
        // e.printStackTrace();
        // }
        return null;
    }
}
